# Secrets / env
.env

# Data inputs (live on Dropbox, not in git)
# NOTE: *.parquet is intentionally NOT ignored — this repo distributes the
# aggregated parquets via raw.githubusercontent.com URLs to downstream apps,
# so they must stay tracked on master.
*.csv
*.xlsx
*.xlsm
*.xls
data/
outputs/

# Derived reference lookup (regenerated from the Census CBSA file)
cbsa_types.json

# Python / OS cruft
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store
